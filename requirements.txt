# Pinned environment for version4 (omnidirectional flock_simulator
# + legacy figure-generation scripts). Python >= 3.10 (we test on 3.12.3).

numpy>=1.24,<3
numba>=0.57
scipy>=1.10           # alpha-stable cross-check in tests
scikit-learn>=1.3     # DBSCAN cross-check diagnostic (analysis only)
matplotlib>=3.6       # figure generation
tqdm>=4.65
imageio>=2.31         # movie utility (legacy run_movie.py)
imageio-ffmpeg>=0.4.9
pytest>=7             # flock_simulator/tests
pytest-cov>=4         # coverage report
mypy>=1.8             # strict type-check of the core package
